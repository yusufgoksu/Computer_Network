from web_client import create_web_client  # Fonksiyonun doğru şekilde import edilmesi
from web_client_advance import WebClient_advance  # WebClient_advance sınıfını import ediyorsunuz
from test_external_server import test_external_server  # WebClient_advance sınıfını import ediyorsunuz

# Web istemcisini başlatma, parametreleri burada doğrudan veriyoruz
print("---- İlk Web İstemcisi Başlatılıyor ----")
create_web_client("127.0.0.1", 80, "/")  # Ana sayfa için "/" kullanıyoruz

print("\n---- Gelişmiş Web İstemcisi Başlatılıyor ----")
client = WebClient_advance("127.0.0.1", 80)  # WebClient_advance sınıfından nesne oluşturuyoruz
client.web_client_advance("/")  # Ana sayfa için "/" kullanıyoruz

# Dış Web Sunucusu Testi Fonksiyonunu Çağırma
test_external_server()
